let Navbar = (()=>{
    return(
        <div>
            <h1>My First Component</h1>
            jsx javascript XML-extention to javascript language syntax
      -JSX tags have tag name and attributes
      -JSX is not a necessity to write React applications
      -JSX makes ur react code simpler and elegant
      -JSX by it self transpiles to pure javascript which is understood by the browser

      ===========JSX differences=========
      -class --> className 
      -for --> htmlFor
      camelCase property naming styles

  
        </div>
    )
})
export default Navbar